{
  "name": "monorepo-sample",
  "workspaces": [
    "web-ui",
    "node-server",
    "packages/*"
  ],
  "scripts": {
    "upgrade-latest": "npx npm-check-updates --workspaces --interactive --target=latest"
  }
}
